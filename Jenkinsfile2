def label = "eigen-jnlp-slave"
podTemplate(label: label) {
    node(label) {
        stage('Run shell') {
            sh 'echo hello world'
	    sh "pwd"
            sh "id"
            sh "kubectl get node"
	    sh "sleep 600"
        }
    }
}
